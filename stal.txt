/* ==================================================
   HANIP RISMAN AB — PORTFOLIO
   Theme: Nature / Forest / Brown / Cream
================================================== */


/* ================= ROOT ================= */

:root {

    --forest: #1d3328;
    --forest-dark: #102219;

    --moss: #52694f;

    --cream: #f3eee3;
    --cream-light: #faf7ef;

    --paper: #e8e0d0;

    --brown: #75553e;
    --brown-dark: #4b3628;

    --gold: #b69b78;

    --text: #1d251f;
    --muted: #6e756c;

    --line: rgba(29, 51, 40, 0.14);

    --serif: "Playfair Display", Georgia, serif;
    --sans: "DM Sans", Arial, sans-serif;
}


/* ================= RESET ================= */

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

html {
    scroll-behavior: smooth;
}

body {
    background: var(--cream);
    color: var(--text);
    font-family: var(--sans);
    line-height: 1.7;
    overflow-x: hidden;
}

a {
    color: inherit;
    text-decoration: none;
}


/* ================= CONTAINER ================= */

.container,
.navbar,
.hero-container,
.intro-container,
.footer-content {
    width: min(1160px, calc(100% - 48px));
    margin: auto;
}


/* ================= HEADER ================= */

.header {
    position: absolute;
    width: 100%;
    top: 0;
    left: 0;
    z-index: 100;
}

.navbar {
    height: 92px;

    display: flex;
    align-items: center;
    justify-content: space-between;

    border-bottom: 1px solid rgba(243, 238, 227, 0.16);

    color: var(--cream);
}


/* LOGO */

.logo {
    font-family: var(--serif);
    font-size: 1.4rem;
    font-weight: 700;
    letter-spacing: 0.08em;
}

.logo span {
    color: var(--gold);
}


/* MENU */

.nav-menu {
    display: flex;
    gap: 34px;

    font-size: 0.75rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
}

.nav-menu a {
    opacity: 0.75;
    transition: 0.3s ease;
}

.nav-menu a:hover {
    opacity: 1;
    color: var(--gold);
}


/* BUTTON NAV */

.nav-button {
    border: 1px solid rgba(243, 238, 227, 0.35);

    padding: 10px 17px;

    border-radius: 50px;

    font-size: 0.75rem;

    transition: 0.3s ease;
}

.nav-button:hover {
    background: var(--cream);
    color: var(--forest);
}


/* ================= HERO ================= */

.hero {
    min-height: 780px;

    padding-top: 150px;
    padding-bottom: 100px;

    position: relative;

    color: var(--cream);

    background:
        radial-gradient(
            circle at 75% 30%,
            rgba(116, 91, 65, 0.35),
            transparent 35%
        ),
        linear-gradient(
            90deg,
            rgba(13, 30, 21, 0.98),
            rgba(22, 43, 31, 0.88),
            rgba(12, 29, 21, 0.96)
        );
}


/* HERO BACKGROUND */

.hero::before {

    content: "";

    position: absolute;

    width: 700px;
    height: 700px;

    left: -250px;
    top: 20px;

    border-radius: 50%;

    background:
        radial-gradient(
            circle,
            rgba(92, 120, 85, 0.28),
            transparent 65%
        );

    pointer-events: none;
}


/* HERO CONTENT */

.hero-container {

    min-height: 600px;

    display: grid;

    grid-template-columns: 1.05fr 0.95fr;

    align-items: center;

    gap: 60px;
}


/* EYEBROW */

.eyebrow {

    display: flex;

    align-items: center;

    gap: 10px;

    font-size: 0.7rem;

    font-weight: 700;

    letter-spacing: 0.2em;

    color: var(--gold);
}

.eyebrow span {

    width: 26px;

    height: 1px;

    background: currentColor;
}


/* HERO TITLE */

.hero h1 {

    margin-top: 18px;
    margin-bottom: 24px;

    font-family: var(--serif);

    font-size: clamp(3.4rem, 6vw, 5.7rem);

    font-weight: 600;

    line-height: 0.98;

    letter-spacing: -0.04em;
}

.hero h1 em {

    color: var(--gold);

    font-style: italic;
}


/* HERO DESCRIPTION */

.hero-description {

    max-width: 540px;

    color: rgba(243, 238, 227, 0.72);

    font-size: 1rem;
}


/* HERO BUTTONS */

.hero-buttons {

    display: flex;

    gap: 12px;

    margin-top: 34px;
}

.btn {

    display: inline-flex;

    align-items: center;

    justify-content: space-between;

    gap: 25px;

    padding: 13px 19px;

    border-radius: 50px;

    font-size: 0.78rem;

    font-weight: 700;

    transition: 0.3s ease;
}

.btn-primary {

    background: var(--cream);

    color: var(--forest);
}

.btn-primary:hover {

    transform: translateY(-4px);

    background: white;
}

.btn-outline {

    border: 1px solid rgba(243, 238, 227, 0.3);

    color: var(--cream);
}

.btn-outline:hover {

    border-color: var(--gold);

    color: var(--gold);
}


/* ================= PHOTO ================= */

.hero-photo {

    min-height: 520px;

    display: flex;

    justify-content: center;

    align-items: center;

    position: relative;
}


/* PHOTO FRAME */

.photo-wrapper {

    width: min(420px, 82%);

    aspect-ratio: 3 / 4;

    padding: 12px;

    position: relative;

    background: rgba(243, 238, 227, 0.09);

    border: 1px solid rgba(243, 238, 227, 0.25);

    transform: rotate(2.5deg);

    box-shadow:
        30px 35px 70px rgba(0, 0, 0, 0.35);

    animation: floating 7s ease-in-out infinite;
}


.photo-wrapper::before {

    content: "";

    position: absolute;

    inset: 5px;

    border: 1px solid rgba(243, 238, 227, 0.14);

    pointer-events: none;

    z-index: 2;
}


/* FOTO */

.photo-wrapper img {

    width: 100%;
    height: 100%;

    display: block;

    object-fit: cover;

    object-position: center;

    filter:
        saturate(0.82)
        contrast(1.03);
}


/* FOTO INFO */

.photo-info {

    position: absolute;

    left: 27px;
    right: 27px;
    bottom: 27px;

    padding: 12px 14px;

    display: grid;

    background: rgba(18, 32, 24, 0.82);

    backdrop-filter: blur(8px);

    border-left: 2px solid var(--gold);
}

.photo-info strong {

    font-size: 0.65rem;

    letter-spacing: 0.14em;
}

.photo-info small {

    color: #b8b8aa;

    font-size: 0.57rem;

    letter-spacing: 0.1em;
}


/* DECOR */

.decor {

    position: absolute;

    color: #8b9c78;

    font-size: 2.8rem;

    opacity: 0.65;
}

.decor-1 {

    top: 8%;
    left: 4%;

    transform: rotate(-20deg);
}

.decor-2 {

    right: 5%;
    bottom: 9%;

    transform: rotate(40deg);
}


/* VERTICAL TEXT */

.vertical-text {

    position: absolute;

    right: -10px;
    top: 50%;

    transform:
        rotate(90deg)
        translateX(50%);

    transform-origin: right center;

    font-size: 0.57rem;

    letter-spacing: 0.34em;

    color: rgba(243, 238, 227, 0.35);
}


/* PHOTO ANIMATION */

@keyframes floating {

    0%,
    100% {
        transform: rotate(2.5deg) translateY(0);
    }

    50% {
        transform: rotate(1deg) translateY(-10px);
    }
}


/* ================= INTRO ================= */

.intro {

    background: var(--brown);

    color: var(--cream);
}

.intro-container {

    min-height: 170px;

    display: grid;

    grid-template-columns: 120px 1fr;

    align-items: center;

    gap: 30px;
}

.intro-number {

    font-family: var(--serif);

    font-size: 2.8rem;

    opacity: 0.55;
}

.intro p {

    max-width: 760px;

    font-size: 1.1rem;
}

.intro strong {

    color: #fff7e9;
}


/* ================= SECTION ================= */

.section {

    padding-top: 120px;

    padding-bottom: 120px;
}


/* SECTION TITLE */

.section-title h2 {

    margin-top: 13px;

    font-family: var(--serif);

    font-size: clamp(2.5rem, 5vw, 4.4rem);

    font-weight: 600;

    line-height: 1.06;

    letter-spacing: -0.035em;
}

.section-title h2 em {

    color: var(--brown);

    font-style: italic;
}

.eyebrow.dark {

    color: var(--brown);
}

.center {

    text-align: center;
}

.center .eyebrow {

    justify-content: center;
}


/* ================= ABOUT ================= */

.about-grid {

    margin-top: 55px;

    display: grid;

    grid-template-columns: 1.1fr 0.9fr;

    gap: 22px;
}


/* ABOUT CARD */

.about-card {

    padding: 46px;

    border: 1px solid var(--line);

    background: rgba(255, 255, 255, 0.24);

    font-size: 1.04rem;
}

.about-card p + p {

    margin-top: 22px;
}

.card-icon {

    margin-bottom: 20px;

    color: var(--brown);

    font-size: 2rem;
}


/* FACTS */

.facts-card {

    padding: 18px 30px;

    border: 1px solid var(--line);

    background: rgba(255, 255, 255, 0.24);
}

.fact {

    min-height: 105px;

    display: grid;

    grid-template-columns: 55px 1fr;

    align-items: center;

    border-bottom: 1px solid var(--line);
}

.fact:last-child {

    border-bottom: none;
}

.fact > span {

    color: var(--brown);

    font-family: var(--serif);

    font-size: 0.75rem;

    font-weight: 600;
}

.fact small {

    display: block;

    color: var(--muted);

    font-size: 0.62rem;

    letter-spacing: 0.15em;
}

.fact strong {

    font-size: 0.94rem;
}


/* ================= SKILLS ================= */

.skills-section {

    position: relative;

    padding-top: 120px;
    padding-bottom: 120px;

    background: #dcd6c7;
}

.skills-section::before {

    content: "CREATE";

    position: absolute;

    right: -30px;
    top: 100px;

    color: rgba(82, 105, 79, 0.06);

    font-family: var(--serif);

    font-size: 10rem;

    font-weight: 700;

    pointer-events: none;
}


.skills-grid {

    margin-top: 55px;

    display: grid;

    grid-template-columns:
        repeat(4, 1fr);

    gap: 15px;
}


/* SKILL CARD */

.skill-card {

    min-height: 330px;

    padding: 25px;

    display: flex;

    flex-direction: column;

    border: 1px solid rgba(29, 51, 40, 0.14);

    background: rgba(243, 238, 227, 0.55);

    transition: 0.3s ease;
}

.skill-card:hover {

    transform: translateY(-7px);

    box-shadow:
        0 18px 35px rgba(29, 51, 40, 0.09);
}


/* FEATURED */

.skill-card.featured {

    background: var(--forest);

    color: var(--cream);
}


/* SKILL HEADER */

.skill-header {

    display: flex;

    justify-content: space-between;

    font-size: 0.6rem;

    letter-spacing: 0.14em;

    opacity: 0.62;
}


/* ICON */

.skill-icon {

    margin-top: auto;

    margin-bottom: 18px;

    color: var(--brown);

    font-size: 3.2rem;
}

.featured .skill-icon {

    color: var(--gold);
}


/* SKILL TITLE */

.skill-card h3 {

    font-family: var(--serif);

    font-size: 1.55rem;

    font-weight: 600;
}

.skill-card p {

    margin-top: 8px;

    color: var(--muted);

    font-size: 0.82rem;

    line-height: 1.7;
}

.skill-card.featured p {

    color: rgba(243, 238, 227, 0.66);
}


/* ================= EXPERIENCE ================= */

.experience {

    background: var(--cream);
}


/* TIMELINE */

.timeline {

    margin-top: 55px;

    border-top: 1px solid var(--line);
}

.timeline-item {

    display: grid;

    grid-template-columns: 110px 1fr;

    gap: 35px;

    padding: 36px 0;

    border-bottom: 1px solid var(--line);
}

.timeline-number {

    color: var(--brown);

    font-family: var(--serif);

    font-size: 1.8rem;

    font-weight: 600;
}


/* LABEL */

.label {

    color: var(--brown);

    font-size: 0.62rem;

    font-weight: 700;

    letter-spacing: 0.17em;
}


/* EXPERIENCE TITLE */

.timeline-content h3 {

    margin-top: 4px;

    font-family: var(--serif);

    font-size: 2rem;

    font-weight: 600;
}

.place {

    color: var(--moss);

    font-size: 0.82rem;

    font-weight: 600;
}

.timeline-content > p:last-child {

    max-width: 670px;

    margin-top: 8px;

    color: var(--muted);
}


/* ================= QUOTE ================= */

.quote-section {

    position: relative;

    padding: 100px 0;

    overflow: hidden;

    background: var(--forest);

    color: var(--cream);
}

.quote-container {

    position: relative;

    z-index: 2;
}

.quote-mark {

    color: var(--gold);

    font-family: var(--serif);

    font-size: 6rem;

    line-height: 0;
}

blockquote {

    max-width: 780px;

    margin-top: 25px;

    font-family: var(--serif);

    font-size: clamp(2rem, 4vw, 3.4rem);

    font-weight: 500;

    line-height: 1.2;
}

.quote-container > p {

    margin-top: 22px;

    color: #aab1a7;

    font-size: 0.8rem;
}


/* ================= CONTACT ================= */

.contact {

    padding: 105px 0;

    background: var(--brown-dark);

    color: var(--cream);
}

.contact-grid {

    display: grid;

    grid-template-columns: 1fr 1fr;

    gap: 100px;

    align-items: end;
}

.eyebrow.light {

    color: var(--gold);
}

.contact h2 {

    margin-top: 14px;

    font-family: var(--serif);

    font-size: clamp(3rem, 6vw, 5rem);

    font-weight: 600;

    line-height: 0.98;

    letter-spacing: -0.04em;
}

.contact h2 em {

    color: var(--gold);

    font-style: italic;
}

.contact-content {

    max-width: 500px;
}

.contact-content > p {

    color: rgba(243, 238, 227, 0.65);
}


/* EMAIL */

.email {

    width: 100%;

    display: flex;

    justify-content: space-between;

    padding: 22px 0 13px;

    margin-top: 18px;

    border-bottom: 1px solid rgba(243, 238, 227, 0.3);

    font-family: var(--serif);

    font-size: 1.1rem;

    transition: 0.3s ease;
}

.email:hover {

    color: var(--gold);

    border-color: var(--gold);
}


/* CONTACT META */

.contact-meta {

    display: flex;

    gap: 25px;

    margin-top: 20px;

    color: #a9a49a;

    font-size: 0.62rem;

    letter-spacing: 0.12em;
}


/* ================= FOOTER ================= */

.footer {

    background: var(--cream);
}

.footer-content {

    min-height: 75px;

    display: flex;

    justify-content: space-between;

    align-items: center;

    color: #7b8178;

    font-size: 0.6rem;

    letter-spacing: 0.13em;
}


/* ==================================================
   RESPONSIVE
================================================== */


/* TABLET */

@media (max-width: 900px) {

    .nav-menu {
        display: none;
    }

    .hero-container {

        grid-template-columns: 1fr;

        padding-top: 30px;
    }

    .hero-photo {

        min-height: 450px;
    }

    .about-grid {

        grid-template-columns: 1fr;
    }

    .skills-grid {

        grid-template-columns:
            repeat(2, 1fr);
    }

    .contact-grid {

        grid-template-columns: 1fr;

        gap: 50px;
    }

}


/* MOBILE */

@media (max-width: 560px) {

    .container,
    .navbar,
    .hero-container,
    .intro-container,
    .footer-content {

        width: calc(100% - 30px);
    }


    /* NAV */

    .navbar {

        height: 76px;
    }

    .nav-button {

        display: none;
    }


    /* HERO */

    .hero {

        min-height: auto;

        padding-top: 115px;
        padding-bottom: 70px;
    }

    .hero h1 {

        font-size: 3.25rem;
    }

    .hero-buttons {

        flex-wrap: wrap;
    }

    .photo-wrapper {

        width: 84%;
    }


    /* INTRO */

    .intro-container {

        grid-template-columns: 1fr;

        gap: 5px;

        padding-top: 35px;
        padding-bottom: 35px;
    }


    /* SECTION */

    .section {

        padding-top: 75px;
        padding-bottom: 75px;
    }


    /* ABOUT */

    .about-card {

        padding: 28px;
    }


    /* SKILLS */

    .skills-section {

        padding-top: 75px;
        padding-bottom: 75px;
    }

    .skills-grid {

        grid-template-columns: 1fr;
    }

    .skill-card {

        min-height: 270px;
    }


    /* EXPERIENCE */

    .timeline-item {

        grid-template-columns: 50px 1fr;

        gap: 12px;
    }

    .timeline-content h3 {

        font-size: 1.5rem;
    }


    /* CONTACT */

    .contact {

        padding: 75px 0;
    }

    .contact-meta {

        flex-direction: column;

        gap: 5px;
    }


    /* FOOTER */

    .footer-content {

        min-height: auto;

        padding-top: 25px;
        padding-bottom: 25px;

        flex-direction: column;

        align-items: flex-start;

        gap: 12px;
    }

}